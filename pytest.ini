[pytest]
timeout = 60
asyncio_mode = auto
testpaths = tests
markers =
    integration: Integration tests
    concept(id): Concept traceability marker
addopts = -m "not integration"
filterwarnings =
    ignore:.*exclude_args.*
    ignore:.*json_encoders.*:DeprecationWarning
